"""selfdrive: a tiny, zero-dependency end-to-end self-driving simulator.

Train a neural network to drive a (simulated) car equipped with 4 cameras on
highway and city tracks, then watch it drive in a browser-based viewer.

This package uses ONLY the Python standard library so it runs anywhere.
"""

__version__ = "1.0.0"
